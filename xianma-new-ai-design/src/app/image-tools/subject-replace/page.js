import PageShell from "@/components/PageShell"
import PlaceholderState from "@/components/PlaceholderState"

export default function SubjectReplacePage() {
  return <PageShell pathname="/image-tools/subject-replace"><PlaceholderState description="主体识别、替换和边缘融合将在这里完成" /></PageShell>
}
