# 复制为 .env 使用。Spike 默认走 mock 网关，无需真实 Key。
# 客户端全程不持有真实 Anthropic Key —— Key 只存公司网关(PRD 8.2.4)。

# 走 mock 网关(默认):
GATEWAY_URL=http://127.0.0.1:8790
# 客户端携带的占位凭证(真机上是网关签发的 JWT，不是 Anthropic Key)
GATEWAY_TOKEN=placeholder-jwt-token
USER_ID=u001

# 若要接真实 Anthropic API(需梦蝶明确授权后再填)：
# 把 GATEWAY_URL 指向真实网关地址，真实 ANTHROPIC_API_KEY 只配在网关侧，不放这里。
